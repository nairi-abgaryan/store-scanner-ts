{
  "name": "store",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "test": "jest --config ./jest.json",
    "start:debug": "ts-node index.ts -- --debug --watch"
  },
  "build": "npm run prebuild && nest build -p tsconfig.build.json",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^28.1.4",
    "jest": "^28.1.2",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.4"
  }
}
